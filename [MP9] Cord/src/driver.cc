#include <cassert>
#include <iostream>
#include <map>

#include "cord-utilities.hpp"
#include "cord.hpp"

int main() {
  // Cord* c1 = new Cord("sse");
  // Cord* c2 = new Cord("ssion");
  // Cord* c3 = new Cord("po");
  // Cord* c4 = new Cord("pre");

  // Cord* c8 = new Cord("nonpo");
  // Cord* c9 = new Cord("ssession");
  // Cord* c10 = ConcatCords(c8, c9);

  // Cord* c11 = new Cord("possession");

  // cs128::ReferenceList<Cord*> cords = {c7, c10, c11};

  // ReduceCords(cords);

  // std::cout << "----------------------" << std::endl;
  // std::cout << "c11 possession address: " << c11 << std::endl;
  // std::cout << "c7 possession address: " << c7->GetRight() << std::endl;

  return 0;
}